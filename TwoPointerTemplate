int left = 0;
int right = -1;
while(left < n) {
    while(right + 1 < n && less) {
       
    }
    if(goodenough) {
        minLength = min(minLength, right - left + 1);
    }
    decrementBit(bits, nums[left]);
    left++;
    if(left > right) {
        right = left - 1;
    }
}
